[phases.setup]
nixPkgs = ["nodejs_24"]

[phases.install]
cmds = ["npm install --prefix frontend", "npm install --prefix backend"]

[phases.build]
cmds = ["npm run build --prefix frontend"]

[start]
cmd = "node backend/server.js"
